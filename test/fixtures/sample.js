// (c) Development Seed

// Tremendous amounts of whitespace in this file.

function baz(bar)

    // Begin function baz
    {
            // Assigning bar to foo
            var foo = bar;

            // Returning foo.
            return foo;

    // End function baz
    }
